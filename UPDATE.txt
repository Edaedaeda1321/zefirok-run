Полный аудит актуальной сборки · 13.08.2026

Исправлено:
- Добавлен .assetsignore. Backend, migrations, tooling, локальные архивы и служебные файлы больше не должны попадать в публичную Static Assets-выгрузку при assets.directory=".".
- Исправлен update.sh: удалена ссылка на отсутствующий scripts/update-and-deploy.sh. Теперь helper запускает штатный npx wrangler deploy и использует существующий custom build из wrangler.jsonc.

Проверено на полной сборке:
- src/worker.js и scripts/check-assets.mjs: синтаксис Node OK.
- Inline JavaScript: index.html, встроенный data-srcdoc игры, rating.html, battle-pass.html, owner.html, profile.html, staff-qr.html — синтаксис OK.
- Статические HTML id активных страниц: дубликатов нет.
- Локальные src/href и встроенные /assets/... ссылки: отсутствующих рабочих файлов не найдено.
- scripts/check-assets.mjs --news-manifest: OK; manifests генерируются.
- scripts/check-assets.mjs --new-only: Asset check passed.
- 290 изображений открываются без ошибок.
- 23 аудиофайла проходят ffprobe.
- Все SQL-миграции применяются последовательно к чистой SQLite БД.
- API-вызовы активных страниц index/rating/battle-pass/owner/staff-qr имеют соответствующие маршруты Worker.
- Worker smoke-test: /api/health отвечает; /src, /migrations, /scripts и wrangler.jsonc закрыты 404.
- В проекте не найдено встроенных Telegram bot token / OpenAI key / private key.
- Исправление верхней лишней строки Battle Pass сохранено.
- Control Center v9.18 и проверка готовности сезона присутствуют.

Не изменялось:
- profile.html (неиспользуемый legacy-файл) — по правилу проекта не трогался.
- Игровая логика, экономика, награды, сюжет, рейтинг, кейсы и D1-схема.

Ручная D1-миграция не нужна.
Деплой: npx wrangler deploy
