Сладкий Забег — deploy repair 20.08.2026

Причина:
LOCAL BUILD 2.0 добавил новые файлы в scripts/, а при замене папки был потерян существующий scripts/check-assets.mjs.
wrangler.jsonc обязательно запускает этот файл перед deploy, поэтому Wrangler завершался MODULE_NOT_FOUND до публикации.

Исправлено:
- восстановлен scripts/check-assets.mjs из последней рабочей версии проекта перед LOCAL BUILD 2.0;
- содержимое восстановлено без изменений;
- Git blob SHA: 77facf445726f3f48e2f6e8ec002a9b626915e6f;
- синтаксис Node.js проверен через node --check.

Измененный файл:
- scripts/check-assets.mjs

Миграции D1 не нужны.

После замены файла выполнить одну команду:
npx wrangler deploy
