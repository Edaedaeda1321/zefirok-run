СЛАДКИЙ ЗАБЕГ — PROD HOTFIX · FAST COMPENSATION CLAIM
Дата: 20.08.2026

ИСПРАВЛЕНО
- Ускорено получение подарков и компенсаций, состоящих только из кейсов (включая массовую выдачу Кейсов Алекса).
- Убрана лишняя синхронная инициализация/сворачивание профиля из экономической доставки обычного кейса.
- Для gift_inbox убрана дублирующая reward_delivery timeline-запись: остаётся единая gift_claim запись после успешной доставки всего подарка.
- После атомарной записи case-only подарка сервер сразу возвращает статус «Получено», не ожидая повторного построения полного склада.
- Склад кейсов обновляется клиентом отдельным быстрым запросом после успешной выдачи.
- После мобильного timeout клиент перепроверяет одновременно и подарок, и склад, чтобы карточка не зависала в «Получаем…».
- Надёжность очереди наград, idempotency и атомарная запись granted_cases сохранены.

ИЗМЕНЕННЫЕ ФАЙЛЫ
- index.html
- src/worker.js

МИГРАЦИИ D1
Нет.

ПРОВЕРКИ
- src/worker.js: node --check — OK.
- index.html embedded/outer executable inline scripts: 7/7 syntax OK.
- Исходный Production index.html: Git blob 78dcf73c9a9c4eb349746d441e30f69fb8a9769c.
- Исходный Production src/worker.js: Git blob fb985a3a193e1a78ceb9b5f4cf5e2628f06cb7a6.

ДЕПЛОЙ
npx wrangler deploy
