Fix for wrangler/esbuild error at src/worker.js around line 26496: Unexpected "else".

Cause:
invalidateSeasonPassConfigCache() was inserted as a separate statement between an if and its else-if branch.

Fix:
wrapped all three branches of setSeasonPassSettingsAction() in braces, so cache invalidation stays inside the corresponding branch.

Validation:
node --check src/worker.js -> PASS

No economy, RNG, prices, rewards, or database behavior was changed by this build fix.
