{
  "name": "zefirok-run",
  "version": "1.0.0",
  "private": true,
  "description": "Build and maintenance tooling for the Sweet Run Telegram Mini App.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/check-production-gate.mjs",
    "check:fast": "node scripts/check-production-gate.mjs --skip-d1",
    "assets:optimize": "node scripts/optimize-assets-webp.mjs"
  },
  "devDependencies": {
    "sharp": "0.35.4"
  }
}
